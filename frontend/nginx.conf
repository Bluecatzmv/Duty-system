user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;
    
    # 开启 gzip 压缩 (优化加载速度)
    gzip on;
    gzip_min_length 1k;
    gzip_comp_level 6;
    gzip_types text/plain application/javascript application/x-javascript text/css application/xml text/javascript;

    # ==================================================================
    # BLOCK 1: HTTP 配置 (当前激活模式：无证书环境)
    # ==================================================================
    server {
        listen 80;
        # 暂时使用 localhost，允许通过 IP 直接访问
        server_name localhost;

        # --- 前端静态文件托管 ---
        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            # 解决 Vue Router History 模式刷新 404 问题
            try_files $uri $uri/ /index.html;
        }

        # --- 后端 API 转发 ---
        location /api/ {
            # http://backend:8000/ 尾部的斜杠很关键，用于去除 /api 前缀
            proxy_pass http://backend:8000/;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            # 暂时告诉后端这是 http 协议
            proxy_set_header X-Forwarded-Proto http;
            
            client_max_body_size 20M;
        }
    }

    # ==================================================================
    # BLOCK 2: HTTPS 配置 (已注释：等待证书配置)
    # 未来有证书后，选中下面所有行，取消注释即可
    # ==================================================================
    # server {
    #     listen 443 ssl;
    #     server_name duty.ops.shiliutv.cn; # 以后记得改回你的域名
    #
    #     # --- SSL 证书配置 ---
    #     ssl_certificate /etc/nginx/certs/server.crt;
    #     ssl_certificate_key /etc/nginx/certs/server.key;
    #
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #     ssl_prefer_server_ciphers on;
    #     ssl_session_cache shared:SSL:10m;
    #     ssl_session_timeout 10m;
    #
    #     # --- 前端 ---
    #     location / {
    #         root /usr/share/nginx/html;
    #         index index.html index.htm;
    #         try_files $uri $uri/ /index.html;
    #     }
    #
    #     # --- 后端 ---
    #     location /api/ {
    #         proxy_pass http://backend:8000/;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto https;
    #         client_max_body_size 20M;
    #     }
    # }

    # ==================================================================
    # BLOCK 3: HTTP 强制跳转 HTTPS (已注释)
    # 未来开启 HTTPS 后，可以把 Block 1 注释掉，开启 Block 2 和 Block 3
    # ==================================================================
    # server {
    #     listen 80;
    #     server_name duty.ops.shiliutv.cn;
    #     return 301 https://$host$request_uri;
    # }
}
